# qqup-showcase 작업 규칙

이 저장소는 qqup의 public 포트폴리오다. 제품 소스 개발 규칙을 복제하지 않고, 공개 문서의 사실성·안전·가독성을 유지한다.

## 작업 시작

1. 변경 전 `README.md`와 `docs/portfolio-scope.md`를 읽는다.
2. 구현 상태를 바꾸는 문장은 private 원본의 코드·테스트·CI 또는 진행 기록으로 확인한다.
3. 확인할 수 없는 내용은 구현 완료로 쓰지 않고 설계, 계획 또는 확인 필요로 표시한다.
4. 변경 작업은 최신 `main` 기준의 `codex/<주제>` 브랜치에서 진행하고 기존 사용자 변경을 보존한다.
5. 쇼케이스에 필요한 가장 작은 범위만 수정한다.

## 브랜치·발행 운영

- 쇼케이스 변경 요청은 주제 브랜치의 관련 커밋과 원격 푸시까지를 기본 완료 범위로 본다. `main`에는 직접 커밋하지 않는다.
- 커밋에는 공개 검토를 마친 관련 파일만 명시적으로 포함하고 private 원본의 변경과 이력을 섞지 않는다.
- 검증을 통과한 커밋은 같은 이름의 원격 주제 브랜치로 푸시하되 강제 푸시는 사용하지 않는다.
- PR 생성, `main` 병합과 외부 배포는 자동 완료 범위가 아니다. `main` 병합은 공개 주장과 검증 상태를 보고한 뒤 사용자의 명시적인 확인을 받아 수행한다.
- 주제 브랜치 푸시를 public 반영 완료로 표현하지 않는다. 완료 보고에는 `작업 브랜치 푸시`, `main 병합`, `GitHub 기본 링크 노출` 상태를 각각 표시한다.
- `main` 병합 후에는 원격 `main` 커밋과 공개 GitHub 문서 내용을 확인한 뒤에만 public 반영 완료로 보고한다.
- 충돌, 공개 범위 위반 또는 출처를 확인할 수 없는 구현 주장이 있으면 커밋·푸시를 중단하고 필요한 결정을 보고한다.

## 저장소 범위

- 허용: 프로젝트 소개, 상위 아키텍처, 기술 판단, 공개 가능한 개발 프로세스, 검증 결과 요약, 스크린샷과 데모.
- 금지: 제품 소스 코드, 전체 스키마, 내부 백로그, 원문 로그, 비밀정보, 실제 환경 변수, 내부 주소와 private 문서 링크.
- private 저장소의 루트·서비스별 `AGENTS.md`를 이 저장소에 복사하지 않는다. 구조와 설계 의도는 `docs/vibe-coding-workflow.md`에서 설명한다.
- 이 파일은 쇼케이스 유지 규칙만 담당하며 실제 제품 구현의 원본 규칙으로 취급하지 않는다.

## 사실성과 상태 표시

- `구현 완료`, `설계 완료`, `다음 구현`, `후속 계획`을 구분한다.
- 테스트·빌드·CI 통과 주장은 검증 범위와 확인일을 함께 갱신한다.
- 기술 버전, 외부 정책과 제공업체 정보는 공개 시점의 공식 자료 또는 private 원본에서 다시 확인한다.
- 예상 수치, 사용자 수, 성능과 비용을 실제 측정 결과처럼 작성하지 않는다.
- private 원본과 충돌하면 public 내용을 임의로 정답으로 만들지 말고 원본을 확인한 뒤 수정한다.

## 공개 안전

- 토큰, 비밀번호, 실제 환경 변수, 내부 호스트·네트워크·백업 위치와 절대 로컬 경로를 기록하지 않는다.
- 인증 우회 조건이나 운영 보안 절차처럼 공격에 바로 활용될 수 있는 세부 정보는 공개하지 않는다.
- 스크린샷과 영상은 테스트 데이터를 사용하고 계정 정보, 알림, 브라우저 북마크와 개인 데이터를 제거한다.
- 이미지·글꼴·영상은 사용 권리와 출처를 확인한다. 확인되지 않은 자산은 추가하지 않는다.
- 성인·민감 콘텐츠는 포트폴리오 예시로 사용하지 않는다.

## 문서 작성

- 공개 독자가 private 저장소 없이 이해할 수 있도록 작성한다.
- private 상대 링크나 내부 파일명을 근거 링크처럼 사용하지 않는다.
- 아키텍처와 프로세스는 필요한 경우 작은 Mermaid 다이어그램으로 설명하되 구현되지 않은 흐름을 현재 상태처럼 보이지 않게 표시한다.
- 같은 사실은 한 문서에서 관리하고 다른 문서에서는 요약과 링크를 사용한다.
- 화면과 데모가 없으면 자리표시용 가짜 이미지를 만들지 않고 추가 조건을 명시한다.

## 검증과 완료 보고

- Markdown 포맷과 모든 로컬 링크를 확인한다.
- 비밀정보 패턴, 절대 로컬 경로, private 링크와 이미지 메타데이터를 점검한다.
- 변경한 공개 주장과 그 근거, 추가하거나 제거한 시각 자료, 실행한 검사를 보고한다.
- 이 저장소와 private 저장소의 변경·커밋·푸시는 서로 분리해 보고한다.
